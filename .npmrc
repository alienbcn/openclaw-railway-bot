# Force git protocol to https for all npm installs
git-protocol=https

# Disable SSH for git dependencies
strict-ssl=false

# Disable all lifecycle scripts to prevent postinstall issues
ignore-scripts=true

