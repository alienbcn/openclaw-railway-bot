{
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": -1,
    "restartPolicyMaxRetryDurationSeconds": 0,
    "startCommand": "npm run build && npm run worker"
  }
}
