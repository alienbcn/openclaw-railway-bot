{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "always",    
    "startCommand": "npm run worker",
        "processType": "worker"
  }
}
